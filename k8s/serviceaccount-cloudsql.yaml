apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudsql-proxy-sa
  namespace: default
  annotations:
    # Workload Identity mapping to GCP service account for Cloud SQL access
    iam.gke.io/gcp-service-account: cloudsql-proxy-sa@ea-plan-seo-project.iam.gserviceaccount.com
  labels:
    app: dese-api
    component: backend
    managed-by: ci
